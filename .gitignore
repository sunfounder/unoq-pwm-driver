# Build / probe scratch
.probe/
build/
*.o
*.d
*.elf
*.bin
*.hex
*.map

# Arduino sketch build output
**/build/

# Editors
.vscode/
.idea/
*.swp

# Library sources copied into the App Lab sketch by deploy.ps1.
# The canonical copies live in src/. Everything the library ships starts with
# UNOQ_, so a glob here cannot miss a newly added file.
applab/sketch/UNOQ_*.h
applab/sketch/UNOQ_*.cpp
